# transactions

Demo Duckstring Inlet — simulates a point-of-sale transaction log. Each run appends a batch of new sales events, growing the table over time.

Deploy to a Catchment:

```bash
duckstring pond deploy
```
