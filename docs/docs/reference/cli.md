---
title: CLI
description: Full command reference for the duckstring / ds CLI.
---

# CLI Reference

:::caution Work in progress
This page is a stub. Content to be written in a later pass — enumerate every command, option, and default from `src/duckstring/cli/`.
:::

**Planned content:**

- Global options (`-c` Catchment selection, completions)
- `duckstring catchment` — init / start / connect / health
- `duckstring pond` — init / demo / deploy (`--all`)
- `duckstring trigger` — tap / pulse / wave / tide / remove; `trigger window {pond} add|list|remove`
- `duckstring control` — wake / sleep (`--upstream`) / force / kill / clear / failure-budget (`--immediate`, `--on-change`)
- `duckstring status [pond]`
- `duckstring get` / `duckstring query` (`--sql`, `--table`, `--csv|--json|--parquet`, `--path`)
- `duckstring config`
